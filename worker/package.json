{
  "name": "sideline-ingest-worker",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Standalone Node ingest worker for Sideline Studio. Pulls master JPEGs from IONOS HiDrive over WebDAV, parses metadata, builds ~1600px clean + watermarked derivatives with sharp, and writes rows + storage objects into Supabase. Runs on a VPS (Node) — NOT bundled into the Cloudflare Workers app.",
  "engines": {
    "node": ">=18.18"
  },
  "scripts": {
    "ingest": "tsx src/index.ts ingest",
    "poll": "tsx src/index.ts poll",
    "serve": "tsx src/index.ts serve",
    "typecheck": "tsc --noEmit",
    "build": "tsc"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.111.0",
    "exifr": "^7.1.3",
    "sharp": "^0.33.5"
  },
  "devDependencies": {
    "@types/node": "^22.16.5",
    "tsx": "^4.19.2",
    "typescript": "^5.8.3"
  }
}
